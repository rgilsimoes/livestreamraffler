rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      //TODO Alterar regra para autenticação
      allow read, write: if true;
    }
  }
}
